# ───── Server ─────
server.port=${PORT:8080}

# ───── PostgreSQL / Supabase ─────
spring.datasource.url=${DATABASE_URL:jdbc:postgresql://aws-1-ap-southeast-1.pooler.supabase.com:5432/postgres?sslmode=require}
spring.datasource.username=${DATABASE_USERNAME:postgres.vyolxcvchiiqdosjfoeb}
spring.datasource.password=${DATABASE_PASSWORD:wealthwise@2004}
spring.datasource.driver-class-name=org.postgresql.Driver

# HikariCP pool tuning for Supabase pooler
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.maximum-pool-size=5
spring.datasource.hikari.minimum-idle=1
spring.datasource.hikari.idle-timeout=300000
spring.datasource.hikari.max-lifetime=600000
spring.datasource.hikari.keepalive-time=60000
spring.datasource.hikari.connection-init-sql=SELECT 1

# ───── JPA ─────
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.open-in-view=false
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true
spring.jpa.properties.hibernate.temp.use_jdbc_metadata_defaults=false
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# ───── JWT ─────
app.jwt.secret=${JWT_SECRET:WealthWiseSecretKey2026SuperSecureRandomStringForHS512}
app.jwt.expiration-ms=604800000

# ───── CORS ─────
app.cors.allowed-origins=${CORS_ORIGINS:http://localhost:5173,http://localhost:5174,http://localhost:5175}

# ───── Cache (Caffeine) ─────
spring.cache.type=caffeine
spring.cache.caffeine.spec=maximumSize=500,expireAfterWrite=10m

# ───── Mail ─────
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME:faishalrahman729580@gmail.com}
spring.mail.password=${MAIL_PASSWORD:jwjs uvde jzsf ovpo}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
